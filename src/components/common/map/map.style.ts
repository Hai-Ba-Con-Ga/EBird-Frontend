import googleMapReact from "google-map-react";
import styled from "styled-components";

export const CustomMap = styled(googleMapReact)`
    border :2px solid var(--dark-blue);
    border-radius: 100px;
`